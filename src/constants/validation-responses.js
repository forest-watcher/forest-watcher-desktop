const required = <span className="form-error -required">Required</span>;

const email = <span className="form-error -email">Email not valid</span>;

const url = <span className="form-error -url">Url not valid</span>;

const passwordConfirmation = <span className="form-error -password-confirmation">Passwords don't match</span>;

const urlTile = <span className="form-error is-visible">Text is not a URL tile layer</span>;

export { required, url, email, passwordConfirmation, urlTile };
